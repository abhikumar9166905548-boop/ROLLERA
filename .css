/* --- 1. Profile Header & Stats (Followers/Following) --- */
.profile-stats-container {
    display: flex;
    justify-content: space-around;
    padding: 20px 10px;
    border-bottom: 1px solid #333;
    text-align: center;
}

.stat-item b {
    display: block;
    font-size: 18px;
    color: #fff;
}

.stat-item span {
    font-size: 13px;
    color: #999;
}

.profile-actions {
    display: flex;
    gap: 10px;
    padding: 10px;
}

.edit-btn, .follow-btn {
    flex: 1;
    background: #333;
    color: white;
    border: none;
    padding: 8px;
    border-radius: 5px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.2s;
}

.edit-btn:active, .follow-btn:active {
    opacity: 0.7; /* Click karne par effect */
}

.follow-btn {
    background: #0095f6; /* Instagram Blue */
}

/* --- 2. Profile Grid: 3x3 Square Photos --- */
#userPostGrid {
    display: grid; 
    grid-template-columns: repeat(3, 1fr); 
    gap: 2px; 
    margin-top: 20px;
}

#userPostGrid img {
    width: 100%;
    aspect-ratio: 1 / 1; /* Photo ko hamesha square rakhega */
    object-fit: cover;
}

/* --- 3. Reels View: Full Screen Snap Effect --- */
#reelsView {
    display: none; /* JS isse show karega */
    scroll-snap-type: y mandatory;
    overflow-y: scroll;
    height: 100vh;
    width: 100%;
    background: #000;
    scrollbar-width: none; /* Firefox ke liye */
    -ms-overflow-style: none; /* IE ke liye */
    -webkit-overflow-scrolling: touch; 
}

/* Chrome/Safari se scrollbar hatane ke liye */
#reelsView::-webkit-scrollbar {
    display: none;
}

.reel-video-container {
    width: 100%;
    height: 100vh; 
    scroll-snap-align: start;
    scroll-snap-stop: always;
    position: relative;
    background: #000;
}

.reel-video-container video {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Video poori screen cover karegi */
}

/* --- 4. Reels Icons Sidebar --- */
.reel-sidebar {
    position: absolute;
    right: 15px;
    bottom: 100px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    align-items: center;
    z-index: 20;
}

.reel-sidebar i {
    font-size: 28px;
    color: white;
    text-shadow: 0 0 5px rgba(0,0,0,0.5);
    cursor: pointer;
}

.reel-sidebar span {
    font-size: 12px;
    color: white;
    margin-top: 5px;
}

/* --- 5. Double Tap Heart Animation --- */
.heart-animation {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(0);
    font-size: 100px;
    color: white;
    opacity: 0;
    pointer-events: none;
    z-index: 15;
}

/* Animation trigger karne ke liye is class ko JS se toggle kar sakte ho */
.heart-pop {
    animation: heartPop 0.8s ease-out;
}

@keyframes heartPop {
    0% { transform: translate(-50%, -50%) scale(0); opacity: 0; }
    50% { transform: translate(-50%, -50%) scale(1.2); opacity: 0.9; }
    100% { transform: translate(-50%, -50%) scale(1); opacity: 0; }
}
